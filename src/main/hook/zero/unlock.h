/**
 * Patch module to force unlock all songs and missions of the game.
 */
#ifndef HOOK_ZERO_UNLOCK_H
#define HOOK_ZERO_UNLOCK_H

/**
 * Initialize the patch module force unlocking all locked game content.
 */
void hook_zero_unlock_init(void);

#endif
